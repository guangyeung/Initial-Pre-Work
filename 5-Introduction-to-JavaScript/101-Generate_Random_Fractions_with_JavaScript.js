// This lesson was about how to use the Math.random() function to generate a random decimal between 0 and 1. I learned that a 0 can be generated but a 1 cannot.

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
