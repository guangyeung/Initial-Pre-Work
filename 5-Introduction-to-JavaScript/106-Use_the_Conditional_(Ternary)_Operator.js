// In this lesson, I learned about the ternary operator. It can be used to rewrite short if / else statement in one line.

function checkEqual(a, b) {
  return a == b ? true : false;
}

checkEqual(1, 2);
